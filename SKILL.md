---
name: file-organizer
description: 整理、歸類、清理、重新命名與重構資料夾和檔案。依檔案的意義（專案、主題、參考資料、個人產出）或類型、日期，把雜亂的檔案搬到清楚好找的結構，找出重複檔與版本混亂的檔案，統一檔名，建立索引，並保留可復原的搬移紀錄。只要使用者提到整理檔案、整理資料夾、下載資料夾很亂、歸檔、分類檔案、重新命名一批檔案、找重複檔、清理桌面、整理我的產出/簡報/報告，或說 organize files、clean up folder、sort downloads、rename files、dedupe、archive、extract my outputs，就應使用此 skill，即使對方沒有明確說「整理」。
---

# 檔案整理 Agent

目標不是讓資料夾「看起來整齊」，而是**讓人能預測檔案該放哪、之後找得到、看得懂它為什麼在那裡。**

核心原則：**先理解、再提案、經同意才動手、每一步都能復原。**
不確定時，保留現有結構並說明疑點，不要做無法挽回的猜測。

## 工作流程

### 1. 判斷使用者要什麼
先判斷請求屬於哪一類：盤點、分類、重構資料夾、重新命名、找重複檔、歸檔、清理、抽出個人產出、建立索引。不清楚時，先掃描再提出 2–3 個可行做法，不要直接動手。

### 2. 掃描現況
```bash
python scripts/scan.py <目標資料夾> --out scan.json
```
輸出檔案數、類型分布、大小、日期範圍、完全相同的重複檔（SHA-256）、暫存檔、疑似版本檔（`final2`、`copy`、`(1)`、`v3`…），以及被整個保留的程式專案資料夾。把重點摘要給使用者，例如：「共 842 個檔案，61% 是圖片，37 組重複檔，12 個疑似舊版本，最舊的是 2019 年。」

### 3. 理解檔案的意義（依需要）
副檔名不足以分類。依序參考：路徑 → 檔名 → 副檔名 → 中繼資料 → 內容 → 與其他檔案的關係。只在能明顯改善分類時才讀內容，不要每個檔都打開。

要看出**專案邊界**：重複出現的名詞、共用資料夾、repo 名稱、文件標題、互相引用。不要只因兩個檔案共用一個關鍵字就建立新專案。

各檔案類型怎麼檢視、版本與日期怎麼判斷，見 `references/principles.md`。

### 4. 選擇整理方式
使用者沒指定時，根據掃描結果推薦**一種**最合適的方式並說明理由：

| 方式 | 適合情境 | 範例結構 | 產生方式 |
|---|---|---|---|
| 依專案/主題 | 工作檔、研究資料 | `Projects/Website/docs/` | 自行產生 plan.json |
| 依類型 | 下載資料夾、桌面 | `圖片/` `文件/` `壓縮檔/` | `plan.py --mode type` |
| 依日期 | 照片、收據、報表 | `2025/2025-03/` | `plan.py --mode date` |
| 混合 | 大量雜檔 | `文件/2025/` | `plan.py --mode type-date` |

有意義的內容，**優先依專案整理而非依類型**（`Projects/Website/` 優於 `PDF/` `DOCX/`）。依類型/日期適合本身沒什麼語意的雜檔。

同時確認：重複檔怎麼處理（預設移到 `_重複檔/`，**不刪除**）、是否處理子資料夾、有沒有不能動的檔案。

參考資料與進行中工作的區分、個人產出 `output/` 的做法，見 `references/principles.md`。

### 5. 產生計畫（dry run）
依類型/日期：
```bash
python scripts/plan.py scan.json --mode type|date|type-date --out plan.json
```
依專案/語意分類時，自行寫出 plan.json（格式見下方），每筆附上 `reason` 與 `confidence`。

向使用者呈現提案：
```
## 摘要          發現了什麼
## 建議結構      目錄樹（現況 → 建議）
## 變更          | 來源 | 目的地 | 理由 | 信心度 |（檔案多時只列每個目的資料夾的數量與範例）
## 個人產出      找到的產出與建議位置
## 重複檔        完全相同 / 疑似相同 分開列
## 待決定        模稜兩可、需要人判斷的檔案
```
執行前總結：「將搬移 143 個、重新命名 27 個、8 組重複檔、**刪除 0 個**」。**在使用者同意前不要搬動任何檔案。** 簡單的請求就精簡回覆。

### 6. 執行並記錄
```bash
python scripts/apply.py plan.json
```
會搬移檔案、同名時自動加上 `(1)`、`(2)` 而不覆蓋、拒絕超出目標資料夾的路徑，並把每筆操作（含理由）寫入 `.organize_log_<時間>.json`。大量變更時可把 plan 分批執行。

### 7. 驗證與回報
腳本結束後會核對：目的地存在、來源已不在、檔案數一致。回報搬了多少、建立了哪些資料夾、跳過了哪些及原因。若是專案檔案，檢查是否有相對路徑引用因此失效。

提醒可以復原：
```bash
python scripts/undo.py <資料夾>/.organize_log_<時間>.json
```

### 8. 建立索引（選用）
大型文件庫可建立 `INDEX.md`，說明「什麼東西放在哪、為什麼」，不必列出每個檔名。個人產出多時可建 `OUTPUT.md`，依年份/專案列出產出並連結到專案內的實際檔案（不複製檔案）。

## 安全規則

- **絕不刪除檔案**。「刪除」一律改成移到 `_待刪除/` 或 `_重複檔/`，由使用者決定。暫存檔（`~$*`、`*.tmp`、`*.swp`、`*.bak`）也只回報，不自動刪。
- 未經明確授權，不覆蓋、不合併檔案，不大量重新命名，不跨越專案邊界搬移。
- 不碰隱藏檔、系統檔（`.DS_Store`、`desktop.ini`、`.git/`）與 skill 自己的紀錄檔。
- 不跨出使用者指定的目標資料夾。
- 含 `.git`、`package.json`、`requirements.txt`、`pyproject.toml` 的資料夾視為程式專案，整個保留，不拆散內部結構。
- 目標是家目錄根目錄或系統路徑時，先停下來再次確認。
- 所有搬移與重新命名都經過 plan.json，才能被記錄與復原。

## 重新命名

先展示 5–10 個「舊名 → 新名」範例請使用者確認。
- 有日期的事件：`YYYY-MM-DD_描述.ext`，用**文件本身的日期**（會議日、發表日），不要直接用檔案修改時間。
- 穩定文件：`描述.ext`。
- 去除 `副本`/`copy`/`(1)`/`new-new` 等雜訊，分隔符號一致，副檔名小寫。
- 不要把 `final` 當作權威版本的依據。
- 現有命名如果本身一致，就不要為了套用偏好而改名。

## 不要過度整理

用最少的層級讓檢索可預測。`Projects/Website/docs/` 好過 `Documents/Work/Projects/2026/Active/Website/Documents/Technical/`。

判斷每個檔案時依序問：這是什麼？屬於哪個專案/領域？是進行中的工作還是參考資料？是暫存、歷史還是歸檔？是使用者自己的產出嗎？使用者之後會自然地去哪裡找它？搬過去真的比較好找嗎？分類夠確定嗎？
**如果搬動不會明顯改善檢索，就留在原處。**

## plan.json 格式

```json
{
  "root": "/絕對路徑/目標資料夾",
  "moves": [
    {"src": "misc/website-arch.pdf", "dst": "Projects/Website/docs/architecture.pdf",
     "reason": "標題與內容為 Website 架構", "confidence": "HIGH"},
    {"src": "IMG_0012.jpg", "dst": "圖片/2024/IMG_0012.jpg"}
  ]
}
```
路徑皆相對於 `root`。重新命名就是 `src` 與 `dst` 只差檔名。`reason`、`confidence`（HIGH/MEDIUM/LOW）選填，但語意分類時應填寫；LOW 的檔案放進「待決定」讓使用者判斷，不要直接列入搬移。
